const path = require("path");

console.log(path.join("Node", "Project1"));

console.log(path.normalize("../../Node/../Bilt in Module fs"));

console.log(path.resolve("../../Node/../Bilt in Module fs"));